# MCP rebuild lifecycle under OTP

Status: planning; implementation authorized by user, pending independent plan review and mandatory TLA+ gate.
Epic: `bd-mcp-rebuild-lifecycle-gbi9`. Base: master `2ba68d6640c40ae254cfeee60dc84432b2f600f5` (standalone epic; implementation in `.state/worktrees/`).

## Problem and boundary

`kb_rebuild` currently casts `PortManager.rebuild_async/0`, which launches `sh -c ... &` and immediately reports that rebuild started. The shell double-fork deliberately leaves `kb rebuild` alive after BEAM exit; OTP cannot observe completion, prevent duplicate jobs, or terminate it. Keep Rust responsible for event-log replay, locking, and index/SQLite computation. Elixir owns the child lifecycle. Preserve all 17 MCP tools and the Option B repository/OS-credential boundary; no package authorization or new dependency.

## Contract

1. `kb_rebuild` remains a no-argument tool with no new public fields. Its existing successful immediate acknowledgment text is emitted only after a synchronous internal launch call has accepted an already-running attempt or successfully opened one child. A failed executable/port launch returns an MCP tool error, not a false “started” result. The acknowledgment does not claim successful completion. With no database, retain the existing init hint and launch nothing.
2. A dedicated OTP-supervised rebuild owner launches the Rust rebuild worker directly, without shell, background ampersand, or detached process. It observes exit status and drains stdout/stderr without unbounded memory or log growth; the existing `rebuild.log` path may be retained with a documented size cap/rotation. The normal `kb mcp` request port remains available while rebuild runs.
3. Bind the worker to **the same database, event log, and lock** as the active `kb mcp --db` port, including canonical layout, explicit `KB_DB_PATH`, and adjacent legacy event/lock fallback. Current `kb rebuild` discovers from cwd and has no `--db`, so merely deriving a working directory by dropping two path components is insufficient. A narrow Rust CLI `--db`/explicit-path change is allowed if necessary; reuse the same path-resolution rule as MCP and keep Rust's replay/index algorithm unchanged. Do not dispatch rebuild over the long-lived synchronous MCP Rust port, which would block ordinary reads.
4. At most one active rebuild per MCP application. Concurrent `kb_rebuild` calls coalesce into that attempt and receive the same acknowledgment. Once the prior child has exited **and termination has been observed**, a later explicit request may start a fresh attempt. No automatic retry: Rust already bounds internal prefix/checkpoint retries, and a nonzero exit can follow partial durable work. Report completion/failure to logs; do not silently claim completion.
5. Provide a **private Elixir cancellation operation** (no new MCP method) that requests child termination and waits for observed OS exit before clearing the active slot or permitting a fresh attempt. Shutdown/cancellation must not leave a worker after normal MCP stdin EOF (`McpServer` calls `System.halt/1`), owner crash, or BEAM termination. Real-process tests must capture child OS PID and check liveness with a bounded wait; Port.info/1 or an Erlang process monitor alone is insufficient. If a direct port cannot guarantee this, use the smallest independently reviewed native mechanism that does, without detached shell supervision.
6. The existing Rust rebuild data-format and transactional behavior remain unchanged. No Rust coordination layer, OPA, package authorization, public cancellation method, or new dependency is introduced.

## Task flow and acceptance

- `.1` TLA+ abstract model under `.state/agent-kb/tla/`: idle/running/cancelling/finished/failed/stopped, duplicate requests, launch failure, owner death, terminal reporting, fresh-request retry; safety: at most one active child, no new start before cancellation observes exit, no orphan after shutdown, no automatic retry; liveness under fair child completion/termination. TLC `-workers auto` and independent reviewer/analyst approval before implementation.
- `.2` Failing ExUnit and real-process regression tests characterize existing acknowledgment/no-db behavior and prove accepted-versus-failed launch, one active job, coalescing, completion, nonzero failure, private cancellation/await, normal EOF, owner crash, BEAM exit, fresh-request retry, and independent ordinary port requests. A controlled fake `kb` captures OS PID; test no orphan rather than merely a closed Erlang port. Test exact database/events/lock binding for canonical, explicit, and adjacent legacy layouts.
- `.3` Implement OTP ownership in Elixir (`Application`, a dedicated rebuild owner, `PortManager`/`McpServer` narrow routing), plus only a narrow Rust path-binding interface if required. Keep the Rust replay/index leaf and the 17 public request schemas unchanged. Bound stdout/stderr buffering and retained log bytes.
- `.4` Verify focused/full Mix tests, process scripts including OS-PID checks, packaged MCP launch, `nix build .#mcp`, formatting, and formal refinement. Run focused/full Rust tests if Rust changes. Confirm real Rust rebuild targets the active MCP store through the package.
- `.5` Run post-implementation gates and independent review; explicit user approval is still required for merge/push.
- `.6` Update `docs/src/mcp.md` and MCP README to state acceptance vs completion and supervised shutdown, and persist reusable findings via KB MCP.

## Design risk and decision

The direct-port probe already showed owner normal stop and `:kill` terminate the Rust child, but whole-BEAM `System.halt(0)` leaves it alive after two seconds. Therefore direct Port ownership alone does **not** discharge the no-orphan invariant. Probe the smallest native lifetime guard **before** selecting one: (a) an opt-in rebuild-only Rust leaf thread that waits for EOF on its inherited Erlang-port stdin and exits the child, provided real probes show that pipe stays open while the owner lives and closes on owner/whole-VM death; or (b) a parent-death signal if the actual child PPID is stable and equals BEAM. For (b), the existing `rustix` dependency has a `process` feature with a parent-death API; capture PPID, arm the fatal signal immediately, then check PPID again to close the setup race. Prefer the portable stdin-EOF guard if it proves the invariant without a new feature/platform restriction. Both are lifetime guards in the Rust leaf, not Rust job coordinators. Prove normal MCP EOF/System.halt, owner stop/:kill, abrupt BEAM exit, and idle-child survival with real PID plus start-time checks. If neither works, pause for independent native-design review; do not weaken no-orphan or introduce a detached watchdog. Do not infer exact path binding from cwd; the Rust MCP worker already has explicit `--db` plus adjacent event/lock fallback, while standalone rebuild currently has neither.
