\* CE2 — SECONDARY COUNTEREXAMPLE: compact.rs:121 never matches
\* ("evidence_expire","evidence"), so compaction drops those events outright and
\* the compacted log resurrects an evidence row the original log had removed.
\*
\* Action set restricted to add-on-absent / evidence_add / evidence_expire /
\* compact (SpecCE2).  Forbidding re-upsert removes CE1's shape from the search
\* space, so the only violation left is the resurrection.
\*
\* MaxLogLen is raised to 5 per the epic plan's budget for this run.  The
\* shortest violating trace turns out to need only 4 events, so the extra
\* headroom is spent, not required.
\*
\* Retained as a NAMED REGRESSION GATE.  It failed against the pre-T1
\* CompactedLogE, which never matched ("evidence_expire","evidence"); against
\* the implemented one it MUST PASS.

SPECIFICATION SpecCE2

CONSTANTS
    EntryIds    = {"e1"}
    EvidenceIds = {"v1"}
    MaxLogLen   = 5

CONSTRAINT LogLenBound

INVARIANT CompactionEquivalenceE

\* Run: tlc AgentKbEvidence -config AgentKbEvidence_CE2.cfg -workers 4 -deadlock
