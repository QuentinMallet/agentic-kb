# Lock Contract

This table records the current opener discipline in `src/` for C2/L1b.

`read-only` means `db::open_ro(...)` and therefore `PRAGMA query_only=ON`.
`write-under-lock` means `db::open_rw(paths, &lock)` with a live `paths.lock`
guard. `init` means `db::open_or_init(...)`. `scratch` means
`db::open_scratch(...)`. `checkpoint` means `db::open_live_for_checkpoint(...)`
— C1/D4's raw live-path opener, used only inside rebuild's swap while holding
`paths.lock`. `legacy-wrapper pending L2/L3/L1c` means a file still contains
`db::open_db(...)`.

References below name the enclosing function (qualified with its struct when
a file has more than one `run`/`execute_with`) instead of a line number, so
they survive a rebase that shifts lines. A line number appears only where a
function name would be ambiguous on its own.

| File | Opener class | Backing reference |
| --- | --- | --- |
| `src/bin/kb-bench-fixture.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` in `fn main`. |
| `src/commands/add.rs` | delegates to `kb_core::add` + legacy-wrapper pending L2/L3/L1c | `execute_with` contains no direct DB opener of its own — all event-writing and DB-apply work is delegated to `kb_core::add` (write-under-lock in `kb_core::add`, `src/components/kb_core.rs`); remaining `open_db` calls are test coverage in `test_cmd_add_old_event_without_permanent_replays` and `test_kb_add_soft_mandate_stores_evidence_less_entries_with_derived_status`. |
| `src/commands/cited_by.rs` | read-only | `db::open_ro` in `CitedBy::run`; first-run empty-result behavior is covered by `tests/first_run_ux.rs`. |
| `src/commands/compact.rs` | mixed: read-only (gate) + write-under-lock + init + legacy-wrapper pending L2/L3/L1c | C1/D3's convergence gate in `Compact::run` probes with `open_ro` first; the locked peer sweep and the VACUUM path's write (`maybe_vacuum_after_compact`) only run once that gate passes — both are skipped, not just the sweep, when no database exists (`tests/first_run_ux.rs::kb_compact_on_a_fresh_repo_stays_a_pure_log_rewrite`). `open_or_init` and `open_db` remain in test helpers/fixtures (`freelist_count`, `seed_through_cursor`, `test_vacuum_fires_after_n_compacts_and_counter_resets`, `test_compact_sweeps_expired_peers_under_the_write_lock`, `test_vacuum_not_fired_when_freelist_below_floor`, `test_sigkill_during_vacuum_leaves_db_readable`). The `kb add` vs. `kb compact` recovery asymmetry (see the row near the bottom of this table) is documented in a comment inside `Compact::run` itself. |
| `src/commands/compress.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` in the top-level `run` function (production) and its `conn2` re-open later in the same function; `test_compress_strands_no_evidence_on_expired_entry_id` and `test_compress_propagates_evidence_row_decode_failure` are test fixtures — the former now seeds through the applied-cursor writer (`open_rw` + `cursor::append_and_apply`) rather than a bare `open_db` write. |
| `src/commands/context.rs` | read-only | `db::open_ro` in `Context::run`; `tests/first_run_ux.rs` covers the uninitialized-read contract shared with other read paths. |
| `src/commands/digest.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` in `read_digest_hash` (production) and `test_digest_session_basic` (test fixture). |
| `src/commands/eval.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` in `Eval::run`; on the uninitialized-read path this command calls `cursor::warn_if_behind` rather than the removed `rebuild_if_schema_obsolete` (C1's `recover_if_needed` replaces it — see the row below). |
| `src/commands/expire.rs` | write-under-lock | `db::open_rw` in `Expire::run`. |
| `src/commands/mcp.rs` | mixed: read-only + write-under-lock + init + legacy-wrapper pending L2/L3/L1c | Read opens in `handle_search` (two: the local index scan, and a second federation-path open later in the same function), `handle_kb_get`, `handle_provenance`, and `handle_kb_peers_list`; locked writes in `handle_import`, `handle_run`, `handle_test_add`, `handle_stale_check`, `handle_expire`, `handle_audit_run`, `handle_audit_record`, `handle_kb_peers_add`, `handle_kb_peers_remove` — several of these now nest inside C1's `cursor::append_and_apply`/`append_and_apply_with` (D3's single write helper) rather than calling `apply_event` directly; `handle_reembed` has no opener of its own, delegating entirely to `crate::commands::reembed::run_reembed`; `handle_audit_report` reads through the legacy `open_db` wrapper rather than `open_ro`; test fixture init in `setup` (the shared test-module helper). C1/D3 also added a pre-dispatch recovery check (`MUTATING_METHODS`, checked in `handle_request`) and a `stale` field on `handle_search`/`handle_kb_get` responses reporting a D3 deferral to the caller instead of only to stderr. |
| `src/commands/migrate_citations.rs` | mixed: read-only (test) + write-under-lock + legacy-wrapper pending L2/L3/L1c | Locked migration write in `apply_heals`; remaining `open_db` calls are the CLI entry point's own read (in the top-level `run` function) and the test helper `setup_repo`; a C1/T4 test assertion in `test_migrate_citations_heals_matching_legacy_whole_file_range` reads through `open_ro` to confirm the heal writer leaves the applied cursor converged. |
| `src/commands/older_than.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` in the top-level `run` function. |
| `src/commands/peers.rs` | mixed: read-only + write-under-lock + init | Read opens in `PeersList::run`, `PeersShow::run`, `PeersEdgeList::run`; locked peer mutations in `PeersAdd::run`, `PeersRemove::run`, `PeersImport::run`, `PeersEdgeAdd::run`, `PeersEdgeRemove::run`, `PeersEdgeCleanupEpic::run`; test setup uses `open_or_init` in `test_peers_read_helpers_filter_expired_rows_without_deleting_them` and `test_peers_import_skips_expired_but_present_duplicate`. |
| `src/commands/rebuild.rs` | mixed: read-only + write-under-lock + scratch + checkpoint + init + legacy-wrapper pending L2/L3/L1c | Read/write opens in `recover_if_needed` (`open_ro` for the gate, `open_rw` on the `ReplayTail` branch); `full_rebuild_for` still uses the deprecated `open_db` opener deliberately, since the write it makes (stamping `schema_version`) is governed by the schema-upgrade lock rather than `paths.lock`; `Rebuild::execute_with` holds the locked write for its own DB touch, opens the scratch DB twice (rebuild pass, then re-verify), and — C1/D4 — calls `checkpoint_live_db`, which opens through `open_live_for_checkpoint` while `paths.lock` is held for the whole swap; test init and remaining `open_db`/`open_rw` sites live in the `tests` module's helpers and in `swap_crash_child_dispatch`, `test_swap_aborts_cleanly_when_checkpoint_stays_busy`, `test_rebuild_restores_cleared_db`, `test_rebuild_with_concurrent_evidence_writes_converges`, `test_rebuild_concurrent_writes_converge`, and `test_rebuild_concurrent_mcp_writers_phase2_barrier`. |
| `src/commands/reembed.rs` | mixed: read-only + write-under-lock + init | Read opens in `run_reembed_with_hooks` and `confirm_embedded_ids_are_live`; locked write in `write_batches`; test setup uses `open_or_init` extensively; `check_embed_mode_vintage` stays a separate contract row below. |
| `src/commands/run.rs` | write-under-lock | `db::open_rw` in the top-level `run` function. |
| `src/commands/search.rs` | mixed: read-only + init + legacy-wrapper pending L2/L3/L1c | Read federation open in `Search::execute_with` (the only production DB access in this file); test init and remaining `open_db`/`open_rw` calls in `test_collect_peer_paths_filters_expired_rows_without_deleting_them`, `test_federated_global_limit_ignores_physically_present_expired_peer`, `test_kb_search_returns_evidence_with_verified_flag`, `test_kb_search_narrow_k_fallback`, `test_federated_search_score_kind_preserved_across_peer_merge`, and `test_federated_search_does_not_modify_peer_database` are all test fixtures. On the uninitialized-read path this command calls `cursor::warn_if_behind` (C1/ADR-7: detect, never repair) rather than the removed `rebuild_if_schema_obsolete`. |
| `src/commands/stale_check.rs` | mixed: write-under-lock + legacy-wrapper pending L2/L3/L1c | Locked auto-heal path in `heal_relocations`; legacy wrapper in `StaleCheck::run` stays pending; test fixtures in `execute_with_relocates_against_paths_root_not_git_repo_root` (`open_or_init` + `open_db`), `heal_relocations_resolves_against_repo_root_on_legacy_layout` (`open_or_init` + `open_rw`), and `test_heal_relocations_leaves_the_cursor_caught_up` (`open_rw` + `open_ro`). |
| `src/commands/test_add.rs` | write-under-lock | `db::open_rw` in `TestAdd::run`. |
| `src/commands/tests.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` in the top-level `run` function. |
| `src/components/db.rs` | contract definitions: read-only + write-under-lock + scratch + checkpoint + init + legacy-wrapper pending L2/L3/L1c | Openers are defined as `open_live_for_checkpoint` (C1/D4 — guarded by `debug_assert!(is_live_db_path(..))`, the deliberate inverse of `open_auxiliary`/`open_scratch`), `open_ro`, `open_rw`, `open_scratch`, `open_or_init`, `open_db`; `tests/open_split.rs` is the opener contract test suite and `tests/open_db_ratchet.rs::connection_open_is_confined_to_the_db_component` gates every raw `Connection::open`/`open_with_flags`/`open_in_memory` site outside this file. |
| `src/components/kb_core.rs` | mixed: write-under-lock + legacy-wrapper pending L2/L3/L1c | Production add path uses `db::open_rw` in `kb_core::add`, then delegates event-writing to C1's `cursor::append_and_apply` (D3's single write helper); remaining `open_db`/`open_rw` sites are tests, in `test_kb_core_add_batch_atomicity_replace_path`, `test_handle_add_delegates_to_kb_core`, and `test_kb_core_add_replace_path_propagates_existing_id_decode_failure`. |
| `query_hits` telemetry exemption | out of scope: separate DB, not the KB DB | `src/commands/search.rs` and `src/commands/context.rs` (both via `query_hits::record_injection`), and `src/commands/mcp.rs` (via `query_hits::record_hits` and `query_hits::counts`) write `paths.query_hits`, which is a different SQLite file rooted by the `query_hits` field on `config::Paths` (`src/config.rs`). |
| `check_embed_mode_vintage` | write to `kb_meta`, contract deferred to L3 | `src/components/db.rs` writes via the caller's connection; the primary call site is inside `apply_event` itself, exercised by every non-noop write path (`cursor::append_and_apply`, `kb_core::add`, MCP handlers) that funnels through it; `reembed.rs`'s `write_batches` also calls it directly rather than by individual command call sites; L3 moves the write under the lock. |
| rebuild schema stamp | schema-upgrade lock, not `paths.lock`; scratch-class exception | Inside `full_rebuild_for`, writes `schema_version` while holding `schema-upgrade.lock`; this is outside the steady-state read/write opener split. |
| rebuild swap precondition | invariant row | In `Rebuild::execute_with`, the "Safety, re-derived" comment must state that MCP's read entry points open through `open_ro` and never take `paths.lock` (no longer a misnomer) and that every MCP mutation opens through `open_rw`, serializing on that lock, held by rebuild across this step — but also that `db::open_db` remains unlocked, load-bearing legacy debt in several CLI commands (`compress.rs`, `digest.rs`, `stale_check.rs`, `eval.rs`, `older_than.rs`, `migrate_citations.rs`, `tests.rs`) until L1c (bd-21ef.2.13), so a concurrent CLI write is the retry-bounded liveness caveat this checkpoint must still tolerate, not merely an in-flight `open_ro` read. |
| read-path temp DB constraint | standing constraint | `db::open_ro` sets `PRAGMA query_only=ON` inside its own body, so no read path may rely on `CREATE TEMP TABLE` or any temp-database write. |
| ADR-7 rebuild `kb_meta` survival | rebuild swap invariant | Rebuild's scratch DB becomes the live DB (the final rename) inside `Rebuild::execute_with`; per ADR-7 (now T5b), keys other than `schema_version` and `embed_text_mode` do not survive that swap, and C1/D3 writes the fresh cursor row itself in that same method immediately after catch-up so recovery does not loop on a cursorless post-rebuild database. |
| C1/D3 recovery vs. `kb compact` asymmetry | observation, not a defect | `kb add` and the other MCP mutating methods auto-recover first (`recover_if_needed`), so a database that is merely behind the log (D3 row 7, `ReplayTail`) is caught up and the write proceeds. `kb compact` (`Compact::run`) does not go through `open_or_init`/`recover_if_needed` at entry, so the same row 7 refuses with "the database is behind the event log" instead of self-healing — deliberate, since compact is destructive to the log itself. |
