# Lock Contract

This table records the current opener discipline in `src/` for C2/L1b.

`read-only` means `db::open_ro(...)` and therefore `PRAGMA query_only=ON`.
`write-under-lock` means `db::open_rw(paths, &lock)` with a live `paths.lock`
guard. `init` means `db::open_or_init(...)`. `scratch` means
`db::open_scratch(...)`. `checkpoint` means `db::open_live_for_checkpoint(...)`
— C1/D4's raw live-path opener, used only inside rebuild's swap while holding
`paths.lock`. `legacy-wrapper pending L2/L3/L1c` means a file still contains
`db::open_db(...)`.

| File | Opener class | Backing reference |
| --- | --- | --- |
| `src/bin/kb-bench-fixture.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` at `src/bin/kb-bench-fixture.rs:71`. |
| `src/commands/add.rs` | delegates to `kb_core::add` + legacy-wrapper pending L2/L3/L1c | `execute_with` contains no direct DB opener of its own — all event-writing and DB-apply work is delegated to `kb_core::add` (write-under-lock at `src/components/kb_core.rs:150`); remaining `open_db` calls are test coverage at `src/commands/add.rs:523` and `:754`. |
| `src/commands/cited_by.rs` | read-only | `db::open_ro` at `src/commands/cited_by.rs:99`; first-run empty-result behavior is covered by `tests/first_run_ux.rs`. |
| `src/commands/compact.rs` | mixed: read-only (gate) + write-under-lock + init + legacy-wrapper pending L2/L3/L1c | C1/D3's convergence gate at `src/commands/compact.rs:116` probes with `open_ro` first; the locked peer sweep at `:126` and the VACUUM path's write at `:392` only run once that gate passes — both are skipped, not just the sweep, when no database exists (`tests/first_run_ux.rs::kb_compact_on_a_fresh_repo_stays_a_pure_log_rewrite`). `open_or_init` and `open_db` remain in tests at `:1675`, `:1797`, `:1862`, `:1875`, `:1942`, `:2034`. The `kb add` vs. `kb compact` recovery asymmetry (see the row near the bottom of this table) is documented in this same comment block at `:93`. |
| `src/commands/compress.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` at `src/commands/compress.rs:58`, `:168` (production); `:440`, `:483` are test fixtures, and `:460` is a test fixture seeding through the applied-cursor writer (`open_rw` + `cursor::append_and_apply`). |
| `src/commands/context.rs` | read-only | `db::open_ro` at `src/commands/context.rs:90`; `tests/first_run_ux.rs` covers the uninitialized-read contract shared with other read paths. |
| `src/commands/digest.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` at `src/commands/digest.rs:224`, `:295`. |
| `src/commands/eval.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` at `src/commands/eval.rs:123`; on the uninitialized-read path this command calls `cursor::warn_if_behind` rather than the removed `rebuild_if_schema_obsolete` (C1's `recover_if_needed` replaces it — see the row below). |
| `src/commands/expire.rs` | write-under-lock | `db::open_rw` at `src/commands/expire.rs:47`. |
| `src/commands/mcp.rs` | mixed: read-only + write-under-lock + init + legacy-wrapper pending L2/L3/L1c | Read opens (`handle_search`, `handle_kb_get`, `handle_provenance`, `handle_kb_peers_list`, and `handle_search`'s second, federation-path open) at `src/commands/mcp.rs:837`, `:897`, `:1138`, `:2279`, `:2473`; locked writes (`handle_import`, `handle_run`/`handle_test_add`/`handle_reembed`/`handle_stale_check`/`handle_expire`/`handle_audit_run`/`handle_audit_record`/`handle_audit_report`/`handle_kb_peers_add`/`handle_kb_peers_remove`) at `:1363`, `:1506`, `:1553`, `:1573`, `:1681`, `:1767`, `:1921`, `:2037`, `:2398`, `:2529` — several of these now nest inside C1's `cursor::append_and_apply`/`append_and_apply_with` (D3's single write helper) rather than calling `apply_event` directly; test fixture init at `:2785`; remaining `open_db` sites are legacy/test surfaces pending later tasks. C1/D3 also added a pre-dispatch recovery check (`MUTATING_METHODS`, `handle_request` at `:90`) and a `stale` field on `handle_search`/`handle_kb_get` responses reporting a D3 deferral to the caller instead of only to stderr. |
| `src/commands/migrate_citations.rs` | mixed: read-only (test) + write-under-lock + legacy-wrapper pending L2/L3/L1c | Locked migration write at `src/commands/migrate_citations.rs:244`; remaining `open_db` calls at `:84` and `:401`; a C1/T4 test assertion reads through `open_ro` at `:504` to confirm the heal writer leaves the applied cursor converged. |
| `src/commands/older_than.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` at `src/commands/older_than.rs:37`. |
| `src/commands/peers.rs` | mixed: read-only + write-under-lock + init | Read opens at `src/commands/peers.rs:192`, `:263`, `:686`; locked peer mutations at `:98`, `:225`, `:440`, `:594`, `:718`, `:757`; test setup uses `open_or_init` at `:993`, `:1063`. |
| `src/commands/rebuild.rs` | mixed: read-only + write-under-lock + scratch + checkpoint + init + legacy-wrapper pending L2/L3/L1c | Read opens at `src/commands/rebuild.rs:131`, `:288`; locked writes at `:164`, `:342`, `:422`; scratch DB opens at `:471`, `:537`; C1/D4's checkpoint-only opener (`open_live_for_checkpoint`) at `:790`, called from `checkpoint_live_db` (`:777`) while `paths.lock` is held for the whole swap; test init at `:1257` (production `mod tests` starts at `:877`); schema-upgrade and test helper `open_db`/`open_rw` sites remain at `:204`, `:984`, `:1182`, `:1283`, `:1472`, `:1515`, `:1587`, `:1660`, `:1945`. |
| `src/commands/reembed.rs` | mixed: read-only + write-under-lock + init | Read opens at `src/commands/reembed.rs:167`, `:305`; locked write at `:348`; test setup uses `open_or_init` extensively; `check_embed_mode_vintage` stays a separate contract row below. |
| `src/commands/run.rs` | write-under-lock | `db::open_rw` at `src/commands/run.rs:72`. |
| `src/commands/search.rs` | mixed: read-only + init + legacy-wrapper pending L2/L3/L1c | Read federation open at `src/commands/search.rs:128` (the only production DB access in this file); test init at `:647`; remaining `open_db`/`open_rw` calls at `:838`, `:921`, `:1012`, `:1078`, `:1108`, `:1198` are all test fixtures. On the uninitialized-read path this command calls `cursor::warn_if_behind` (C1/ADR-7: detect, never repair) rather than the removed `rebuild_if_schema_obsolete`. |
| `src/commands/stale_check.rs` | mixed: write-under-lock + legacy-wrapper pending L2/L3/L1c | Locked auto-heal path at `src/commands/stale_check.rs:247`; legacy wrapper at `:208` stays pending; test fixtures at `:769`/`:770` (`open_or_init` + `open_db`), `:1549` (`open_rw`), `:1575` (`open_ro`). |
| `src/commands/test_add.rs` | write-under-lock | `db::open_rw` at `src/commands/test_add.rs:76`. |
| `src/commands/tests.rs` | legacy-wrapper pending L2/L3/L1c | `db::open_db` at `src/commands/tests.rs:31`. |
| `src/components/db.rs` | contract definitions: read-only + write-under-lock + scratch + checkpoint + init + legacy-wrapper pending L2/L3/L1c | Openers are defined at `src/components/db.rs:284` (`open_live_for_checkpoint`, C1/D4 — guarded by `debug_assert!(is_live_db_path(..))`, the deliberate inverse of `open_auxiliary`/`open_scratch`), `:334` (`open_ro`), `:480` (`open_rw`), `:502` (`open_scratch`), `:571` (`open_or_init`), `:618` (`open_db`); `tests/open_split.rs` is the opener contract test suite and `tests/open_db_ratchet.rs::connection_open_is_confined_to_the_db_component` gates every raw `Connection::open`/`open_with_flags`/`open_in_memory` site outside this file. |
| `src/components/kb_core.rs` | mixed: write-under-lock + legacy-wrapper pending L2/L3/L1c | Production add path uses `db::open_rw` at `src/components/kb_core.rs:150`, then delegates event-writing to C1's `cursor::append_and_apply` (D3's single write helper); remaining `open_db` sites at `:789`, `:1031`, `:1065` are tests. |
| `query_hits` telemetry exemption | out of scope: separate DB, not the KB DB | `src/commands/search.rs:271`, `src/commands/context.rs:129`, and `src/commands/mcp.rs` (e.g. `:947`) write `paths.query_hits`, which is a different SQLite file rooted by `config.rs:277`. |
| `check_embed_mode_vintage` | write to `kb_meta`, contract deferred to L3 | `src/components/db.rs:1150` writes via the caller's connection; the sole call site is now inside `apply_event` itself (`db.rs:1554`), exercised by every non-noop write path (`cursor::append_and_apply`, `kb_core::add`, MCP handlers, `reembed.rs:355`) rather than by individual command call sites; L3 moves the write under the lock. |
| rebuild schema stamp | schema-upgrade lock, not `paths.lock`; scratch-class exception | `src/commands/rebuild.rs:197-226` writes `schema_version` while holding `schema-upgrade.lock`; this is outside the steady-state read/write opener split. |
| rebuild swap precondition | invariant row | At `src/commands/rebuild.rs:611-635`, the "Safety, re-derived" comment must state that MCP's read entry points open through `open_ro` and never take `paths.lock` (no longer a misnomer) and that every MCP mutation opens through `open_rw`, serializing on that lock, held by rebuild across this step — but also that `db::open_db` remains unlocked, load-bearing legacy debt in several CLI commands (`compress.rs`, `digest.rs`, `stale_check.rs`, `eval.rs`, `older_than.rs`, `migrate_citations.rs`, `tests.rs`) until L1c (bd-21ef.2.13), so a concurrent CLI write is the retry-bounded liveness caveat this checkpoint must still tolerate, not merely an in-flight `open_ro` read. |
| read-path temp DB constraint | standing constraint | `db::open_ro` sets `PRAGMA query_only=ON` at `src/components/db.rs:348`, so no read path may rely on `CREATE TEMP TABLE` or any temp-database write. |
| ADR-7 rebuild `kb_meta` survival | rebuild swap invariant | Rebuild's scratch DB becomes the live DB at `src/commands/rebuild.rs:658`; per ADR-7 (now T5b), keys other than `schema_version` and `embed_text_mode` do not survive that swap, and C1/D3 writes the fresh cursor row itself at `:560-574` immediately after catch-up so recovery does not loop on a cursorless post-rebuild database. |
| C1/D3 recovery vs. `kb compact` asymmetry | observation, not a defect | `kb add` and the other MCP mutating methods auto-recover first (`recover_if_needed`), so a database that is merely behind the log (D3 row 7, `ReplayTail`) is caught up and the write proceeds. `kb compact` (`src/commands/compact.rs:93`) does not go through `open_or_init`/`recover_if_needed` at entry, so the same row 7 refuses with "the database is behind the event log" instead of self-healing — deliberate, since compact is destructive to the log itself. |
